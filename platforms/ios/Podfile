# Uncomment the next line to define a global platform for your project
platform :ios, '13.0'

target 'TrustArcMobileApp' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TrustArcMobileApp
  pod 'TrustArcConsentSDK', :git => 'https://YOUR_TRUSTARC_TOKEN@github.com/trustarc/trustarc-mobile-consent.git', :branch => 'release'
  
  target 'TrustArcMobileAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TrustArcMobileAppUITests' do
    # Pods for testing
  end

end
