<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_main"
    android:orientation="vertical"
    tools:context=".MainActivity">

    <!-- Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/main_color"
        android:orientation="vertical"
        android:padding="@dimen/padding_standard">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="TrustArc SDK Testing"
            android:textColor="@color/text_on_dark"
            android:textSize="@dimen/text_size_header"
            android:fontFamily="sans-serif-medium" />

    </LinearLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="@dimen/padding_standard">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- Section 1: Consent Status Dashboard -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="@dimen/margin_standard"
                android:background="@color/background_section"
                android:orientation="vertical"
                android:padding="@dimen/padding_standard">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginBottom="@dimen/margin_standard"
                    android:fontFamily="sans-serif-medium"
                    android:text="Consent Status Dashboard"
                    android:textColor="@color/primary_dark"
                    android:textSize="@dimen/text_size_header" />

                <!-- Placeholder text for when no consents exist -->
                <TextView
                    android:id="@+id/tvConsentPlaceholder"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Consents not set"
                    android:textAlignment="center"
                    android:textColor="@color/status_gray"
                    android:textSize="@dimen/text_size_body"
                    android:fontFamily="sans-serif-medium"
                    android:padding="@dimen/padding_standard"
                    android:visibility="gone" />
                
                <!-- Dynamic status cards will be added here -->
                <LinearLayout
                    android:id="@+id/statusCardContainer"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical">
                        
                </LinearLayout>

            </LinearLayout>

            <!-- Section 2: SDK Controls -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@color/background_section"
                android:orientation="vertical"
                android:padding="@dimen/padding_standard">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginBottom="@dimen/margin_standard"
                    android:fontFamily="sans-serif-medium"
                    android:text="SDK Controls"
                    android:textColor="@color/primary_dark"
                    android:textSize="@dimen/text_size_header" />

                <Button
                    android:id="@+id/btnShowConsentDialog"
                    android:layout_width="match_parent"
                    android:layout_height="@dimen/button_height_primary"
                    android:layout_marginBottom="@dimen/margin_standard"
                    android:background="@drawable/button_primary"
                    android:fontFamily="sans-serif-medium"
                    android:text="Show Consent Dialog"
                    android:textColor="@color/text_on_dark" />

            </LinearLayout>

        </LinearLayout>

    </ScrollView>

</LinearLayout>